interface:
  display_name: "Fentaris Project Setup"
  short_description: "Create minimal Fentaris proxy projects"
  default_prompt: "Use $fentaris-project-setup to create a minimal Fentaris proxy project with CLI-first setup, supported auth/secrets, and validation."
